# Full local/ocr/extract.py pipeline (Docling + YOLO + QwenVL + RapidOCR).
# opencv-python-headless is installed in the Dockerfile before this file (ultralytics
# may otherwise pull opencv-python which needs GUI libs like libxcb).
wordninja>=2.0.0
docling>=2.0.0
transformers>=4.40.0
ultralytics>=8.0.0
rapidocr-onnxruntime>=1.3.0
ollama>=0.3.0
